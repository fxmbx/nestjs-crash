import { RefreshTokenGuardGuard } from './refresh-token-guard.guard';

describe('RefreshTokenGuardGuard', () => {
  it('should be defined', () => {
    expect(new RefreshTokenGuardGuard()).toBeDefined();
  });
});
