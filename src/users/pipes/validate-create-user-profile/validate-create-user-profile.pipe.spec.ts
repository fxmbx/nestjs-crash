import { ValidateCreateUserProfilePipe } from './validate-create-user-profile.pipe';

describe('ValidateCreateUserProfilePipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateUserProfilePipe()).toBeDefined();
  });
});
